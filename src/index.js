import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Renderiza o App dentro do div com id "root" no index.html
ReactDOM.render(<App />, document.getElementById('root'));